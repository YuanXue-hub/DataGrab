# 选择器智能配置改进需求

## 1. 背景

DataGrab 当前支持"只填 name + url"创建数据源，系统自动检测 CSS 选择器。但实际测试暴露两个核心短板：

1. **检测不验证**：`detect_selectors()` 只分析 HTML 结构生成选择器，不实际试抓取验证效果，可能产出"看起来对、实际抓不到"的选择器。
2. **爬取时不兜底**：`GenericScraper` 只读取 source.selectors，若字段为空或检测失败，直接回退到默认 `a` 标签，导致只抓 1 条且多为噪声。
3. **前端无闭环**：用户无法看到自动检测的结果、无法手动微调、无法在保存前预览抓取效果。

本需求通过 P0（智能检测增强）和 P1（选择器测试预览）两个阶段，将"只填 URL"的默认体验从"基本不可用"提升到"开箱可用 + 可调优"。

## 2. 现状分析

### 2.1 已有能力

| 模块 | 路径 | 作用 | 状态 |
|------|------|------|------|
| `selector_presets.py` | [scrapers/selector_presets.py](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/scrapers/selector_presets.py) | 按 hostname 匹配预置选择器，含 ~15 个站点 + 通用兜底 | ✅ 可用 |
| `selector_detector.py` | [scrapers/selector_detector.py](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/scrapers/selector_detector.py) | HTML 结构分析自动检测 article/title/link/content/date/tags 选择器 | ✅ 可用 |
| `sources.py` 创建集成 | [server/routes/sources.py#L106-L121](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/server/routes/sources.py#L106-L121) | 创建数据源时先 detect_selectors，失败回退 get_selectors | ✅ 可用 |

### 2.2 差距清单

| 编号 | 差距 | 影响 | 归属 |
|------|------|------|------|
| G1 | detector 检测后不试抓取验证 | 选择器"看起来对"但实际抓不到 | P0 |
| G2 | GenericScraper 读取空 selectors 时不调用 detector/preset | 手动改空或检测失败时只抓 1 条 | P0 |
| G3 | presets 模板库仅 ~15 个站点，新站点回退到通用兜底 | 通用兜底 `li, article, .post` 噪声多 | P0 |
| G4 | 无 `/api/sources/preview` 预览接口 | 用户保存前看不到抓取效果 | P1 |
| G5 | 前端 SourcesView 无 selectors 编辑表单 | 用户无法手动微调选择器 | P1 |
| G6 | 前端创建数据源时不展示"自动检测到的选择器" | 用户不知道系统配了什么 | P1 |
| G7 | detector 对 JS 渲染页面（如环球网）失效 | 列表页或详情页可能为空 | P0（标注 + 降级） |

## 3. P0 需求：智能检测增强

### 3.1 功能点

| 编号 | 功能点 | 验收标准 |
|------|--------|---------|
| P0-1 | detector 增加"试抓取验证"环节 | 检测后实际抓取前 3 条，标题长度 ≥ 6 且链接有效比例 ≥ 60% 才视为通过 |
| P0-2 | GenericScraper 兜底集成 | `selectors` 为空或 article_selector 缺失时，自动调用 `detect_selectors(url)`，再失败用 `get_selectors(url)` |
| P0-3 | presets 模板库扩充至 30+ 站点 | 覆盖国内主流（凤凰/澎湃/界面/第一财经/财新/央视/中国军网/参考消息）+ 国外主流（CNN/NYT/Al Jazeera/NDTV/Guardian/Reuters/AP/Bloomberg）+ 科技（TechCrunch/The Verge/Hacker News） |
| P0-4 | 检测元数据记录 | source 表新增 `selector_source` 字段（detector/preset/fallback/manual），创建/更新时写入 |
| P0-5 | JS 渲染页面降级标注 | detector 检测到 `<noscript>` 或正文 < 150 字时，标记 `js_rendered: true`，前端提示"建议配置专用 content_selector 或启用 JS 渲染" |
| P0-6 | 爬取时选择器失效自动重试 | GenericScraper 用当前 selectors 抓到 0 条时，自动调用 detector 重新检测一次并重试 |

### 3.2 详细设计

#### 3.2.1 试抓取验证（P0-1）

在 `selector_detector.py` 新增 `validate_selectors(url, selectors, sample_size=3)` 函数：用传入 selectors 实际抓取列表页前 N 条，返回 `{valid_count, total_count, samples, passed}`。设计思路为"检测 + 试抓"双重验证，避免结构正确但语义错误的选择器；实现方法为复用 GenericScraper 的列表页提取逻辑，对前 3 条校验标题长度、链接同域、链接含日期或文章特征；性能指标为单次验证 < 3 秒（不含详情页），验证通过率目标 ≥ 70%。

`detect_selectors()` 末尾调用 `validate_selectors()`，未通过则返回 None，由调用方回退到 preset。

#### 3.2.2 GenericScraper 兜底集成（P0-2、P0-6）

修改 [generic_scraper.py](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/scrapers/generic_scraper.py) 的 `scrape()` 方法开头：

```python
# 选择器兜底链：self.selectors → detect_selectors(url) → get_selectors(url)
if not self.selectors.get("article_selector"):
    from scrapers.selector_detector import detect_selectors
    detected = detect_selectors(url)
    if detected:
        self.selectors = detected
        self.logger.info(f"Selectors auto-detected: {detected}")
    else:
        from scrapers.selector_presets import get_selectors
        self.selectors = get_selectors(url)
        self.logger.info(f"Selectors from preset: {self.selectors}")
```

抓取 0 条时的重试逻辑：在 Step 2 结束后判断 `len(articles) == 0`，调用 `detect_selectors(url)` 重新检测，覆盖 `self.selectors` 后重跑 Step 1-2，最多重试 1 次。

#### 3.2.3 presets 模板库扩充（P0-3）

在 [selector_presets.py](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/scrapers/selector_presets.py) 的 `PRESETS` 字典新增以下条目（选择器基于实际页面结构测试）：

**国内新闻（新增 8 个）**：凤凰网、澎湃新闻、界面新闻、第一财经、财新网、央视网、中国军网、参考消息

**国外新闻（新增 8 个）**：CNN、NYT、Al Jazeera、NDTV、Guardian、Reuters、AP、Bloomberg

**科技/社区（新增 3 个）**：TechCrunch、The Verge、Hacker News

每个条目需包含 `article_selector`、`title_selector`、`link_selector`、`content_selector`、`date_selector` 五个字段，`link_filter`/`summary_selector`/`tags_selector` 按站点结构可选。

#### 3.2.4 检测元数据字段（P0-4）

source 表新增列：

```sql
ALTER TABLE source ADD COLUMN selector_source VARCHAR(20) DEFAULT 'manual'
    COMMENT '选择器来源: detector|preset|fallback|manual';
```

创建数据源时按实际来源写入：`detect_selectors` 成功 → `detector`；回退 `get_selectors` 且命中 PRESETS → `preset`；回退到 FALLBACK → `fallback`；用户手动编辑 → `manual`。

#### 3.2.5 JS 渲染检测（P0-5）

在 `detect_selectors()` 返回前，抓取 1 个详情页检查：
- 正文文本 < 150 字 → 标记 `js_rendered: true`
- 页面 `<noscript>` 含 "enable JavaScript" → 标记 `js_rendered: true`

`js_rendered` 字段放入 selectors JSON 中，前端在数据源详情页展示警告提示。

### 3.3 P0 验收清单

- [ ] 只填 URL 创建 5 个新站点（凤凰/澎湃/CNN/Al Jazeera/TechCrunch），detector 或 preset 命中率 100%
- [ ] 试抓取验证通过率 ≥ 70%（验证 10 个站点，≥ 7 个通过）
- [ ] GenericScraper 抓取 0 条时自动重试，重试后抓取成功率 ≥ 50%
- [ ] source 表 selector_source 字段正确记录来源
- [ ] JS 渲染页面（如环球网）标记 `js_rendered: true` 且前端有提示

## 4. P1 需求：选择器测试预览

### 4.1 功能点

| 编号 | 功能点 | 验收标准 |
|------|--------|---------|
| P1-1 | 新增 `POST /api/sources/preview` 接口 | 输入 url + 可选 selectors，返回前 3 条抓取预览（标题、链接、摘要、正文片段、检测到的选择器） |
| P1-2 | 接口支持"仅 URL"模式 | selectors 为空时自动调用 detector，返回检测结果 + 试抓取样本 |
| P1-3 | 接口支持"URL + selectors"模式 | 用户传入 selectors 时跳过检测，直接用传入选择器试抓 |
| P1-4 | 前端 SourcesView 新增"测试选择器"按钮 | 数据源列表每行加"测试"操作，点击弹出预览抽屉 |
| P1-5 | 前端新增数据源表单支持手动填写 selectors | 创建/编辑表单增加 selectors JSON 编辑器（折叠区，默认收起） |
| P1-6 | 前端创建时展示自动检测结果 | 创建成功后 toast 提示"自动检测到选择器：article_selector=xxx，试抓 3 条全部成功" |
| P1-7 | 前端预览抽屉展示抓取样本 | 抽屉左侧展示 3 条样本（标题/链接/摘要/正文前 200 字），右侧展示当前 selectors JSON |

### 4.2 API 设计

#### 4.2.1 POST /api/sources/preview

**请求体**：

```json
{
    "url": "https://www.example.com",
    "selectors": {
        "article_selector": "li.news-item",
        "title_selector": "h3 a",
        "link_selector": "a",
        "content_selector": ".article-body"
    },
    "sample_size": 3
}
```

`selectors` 可选，为空时触发自动检测；`sample_size` 默认 3，最大 5。

**响应体**：

```json
{
    "success": true,
    "url": "https://www.example.com",
    "selector_source": "detector",
    "selectors": {
        "article_selector": "li.news-item",
        "title_selector": "h3 a",
        "link_selector": "a",
        "content_selector": ".article-body",
        "date_selector": "time"
    },
    "js_rendered": false,
    "samples": [
        {
            "title": "样本文章标题 1",
            "url": "https://www.example.com/news/2026-08-12/story-1",
            "summary": "摘要前 200 字...",
            "content_preview": "正文前 200 字...",
            "content_length": 1523,
            "published_at": "2026-08-12"
        }
    ],
    "validation": {
        "total": 3,
        "valid": 3,
        "passed": true
    },
    "elapsed_ms": 1850
}
```

`selector_source` 取值：`detector`（自动检测成功）、`preset`（命中预设）、`fallback`（通用兜底）、`manual`（用户传入）。

#### 4.2.2 路由实现位置

新增 [server/routes/sources.py](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/server/routes/sources.py) 的 `/sources/preview` 路由，复用 `selector_detector.detect_selectors` + `validate_selectors` + `GenericScraper` 列表页提取逻辑。

### 4.3 前端设计

#### 4.3.1 SourcesView 改造

**数据源列表操作列**新增"测试"按钮，点击调用 `POST /api/sources/preview`（传当前行的 url + selectors），打开抽屉展示预览结果。

**新增/编辑数据源对话框**改造：
- 保留 name + url + description 必填项
- 增加"高级配置"折叠区，展开后显示 selectors JSON 编辑器（monaco-editor 或 el-input textarea + JSON 校验）
- 增加"测试"按钮，点击调用 preview 接口，下方实时展示预览结果
- 创建成功后 toast 展示 `selector_source` 和验证结果

#### 4.3.2 预览抽屉布局

采用左右分栏（数据查看场景适用左右分布）：
- **左侧（60%）**：3 条样本卡片，每张卡片显示标题、链接（可点击）、摘要、正文前 200 字、content_length、发布时间
- **右侧（40%）**：当前 selectors JSON（可编辑 + 复制）、selector_source 标签、js_rendered 警告（如有）、验证结果（valid/total）

#### 4.3.3 类型定义

在 [frontend/src/types/index.ts](file:///Users/Yuan/xy_workspace/2.projects/python/DataGrab/frontend/src/types/index.ts) 新增：

```typescript
export interface PreviewRequest {
    url: string;
    selectors?: Record<string, string>;
    sample_size?: number;
}

export interface PreviewSample {
    title: string;
    url: string;
    summary: string;
    content_preview: string;
    content_length: number;
    published_at?: string;
}

export interface PreviewResponse {
    success: boolean;
    url: string;
    selector_source: 'detector' | 'preset' | 'fallback' | 'manual';
    selectors: Record<string, string>;
    js_rendered: boolean;
    samples: PreviewSample[];
    validation: { total: number; valid: number; passed: boolean };
    elapsed_ms: number;
}
```

### 4.4 P1 验收清单

- [ ] `POST /api/sources/preview` 接口可用，仅传 url 时返回 detector 结果 + 3 条样本
- [ ] 接口响应时间 < 5 秒（含详情页抓取）
- [ ] 前端数据源列表"测试"按钮可打开预览抽屉
- [ ] 前端新增数据源时填 URL + 点测试，能实时看到检测结果和样本
- [ ] 前端 selectors JSON 编辑器支持手动修改 + 重新测试
- [ ] 创建成功后 toast 提示选择器来源和验证结果

## 5. 实施优先级与覆盖率

### 5.1 实施顺序

| 阶段 | 任务 | 依赖 |
|------|------|------|
| P0-1 | detector 试抓取验证 | 无 |
| P0-3 | presets 模板库扩充 | 无 |
| P0-2 | GenericScraper 兜底集成 | P0-1 |
| P0-6 | 爬取 0 条自动重试 | P0-2 |
| P0-4 | source 表 selector_source 字段 | 无 |
| P0-5 | JS 渲染检测 | P0-1 |
| P1-1~P1-3 | preview API | P0-1 |
| P1-4~P1-7 | 前端改造 | P1-1 |

### 5.2 需求覆盖率

| 改进点 | 对应需求 | 覆盖状态 |
|--------|---------|---------|
| 只填 URL 默认体验提升 | P0-1/2/3/6 | ✅ 完整覆盖 |
| 选择器可手动微调 | P1-5 | ✅ 完整覆盖 |
| 保存前预览效果 | P1-1/2/3/4/7 | ✅ 完整覆盖 |
| 常见站点一键可用 | P0-3 | ✅ 完整覆盖 |
| JS 渲染页面识别 | P0-5 | ✅ 完整覆盖 |
| 检测来源可追溯 | P0-4 | ✅ 完整覆盖 |
| Playwright JS 渲染 | — | ❌ 不在本期范围（P2 后续） |

### 5.3 不在本期范围

以下改进不纳入 P0/P1，作为 P2 后续规划：

- **Playwright/Selenium 无头浏览器**：彻底解决 JS 渲染页面，成本较高，待 P0/P1 验证后评估
- **RSS 类型数据源支持**：与选择器配置无关，独立需求
- **选择器模板管理后台**：presets 目前硬编码，后续可改为数据库管理
- **多语言选择器规则库**：按语言区域定制 detector 策略

## 6. 风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| detector 对部分站点结构误判 | 中 | 抓到噪声或 0 条 | P0-1 试抓验证 + P0-6 自动重试 + P1 手动微调三重保险 |
| presets 选择器随网站改版失效 | 低 | 抓取 0 条 | P0-6 自动重试切到 detector，且前端测试按钮可即时发现 |
| preview 接口响应慢（详情页多） | 中 | 用户体验差 | sample_size 限制 ≤ 5，详情页并行抓取，超时 5 秒 |
| 用户手动填写 selectors 语法错误 | 中 | 抓取失败 | 前端 JSON 校验 + 后端 try/except 兜底 |
