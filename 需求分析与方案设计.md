# DataGrab 数据爬虫控制台

## 1. 项目介绍

DataGrab 是一个多源数据采集与情报管理平台，支持从新闻网站、API 接口等多种数据源自动抓取内容，
提供数据源管理、异步爬取任务、数据浏览和导出等完整功能。

**技术架构**：
- 后端：Python + FastAPI + MySQL（pymysql）
- 前端：Vue 3 + TypeScript + Vite + Element Plus
- 爬虫引擎：httpx + BeautifulSoup4 + Readability 算法

## 2. 需求分析

平台的核心功能包含：数据源管理、连通性测试、爬虫任务管理、数据查看、数据导出。

### 2.1 数据源管理

用户可以查询和管理已配置的数据源，支持完整 CRUD 操作。

**功能点**：
- 数据源列表展示（名称、URL、类型、选择器来源、启用状态、创建时间）
- 新增数据源：用户输入名称 + URL，系统自动检测类型（web / api）和 CSS 选择器；可选手动传入 selectors 覆盖自动检测结果
- 编辑数据源：修改 URL、描述、启用状态、selectors
- 删除数据源：级联删除其下所有爬取结果
- 启用 / 禁用切换
- 按名称 / URL 模糊搜索、按类型筛选

**数据源类型**：
- `web`：HTML 网页，通过 CSS 选择器提取文章列表，再进入详情页提取正文
- `api`：API 接口，支持 tRPC 协议（如 RedroomCN）和通用 REST API
- `rss`：RSS 订阅源（规划中）

**选择器来源标记**（`selector_source` 字段）：
- `manual`：用户手动传入 selectors
- `detector`：通过 HTML 结构分析自动检测，且通过试抓取验证
- `preset`：命中预设模板库（按 hostname 精确/后缀匹配）
- `fallback`：未命中预设时使用通用兜底选择器

### 2.2 连通性测试

用户在配置数据源后，可测试目标 URL 的连通性，返回 HTTP 状态码和响应延迟。

### 2.3 爬虫任务管理

用户可以创建异步爬取任务，监控任务状态，查看历史任务。

**功能点**：
- 触发爬取任务：选择数据源 + 设置抓取条数上限，后台异步执行
- 任务状态监控：pending → running → completed / failed，前端 2 秒轮询
- 任务历史列表：从数据库持久化读取，支持按数据源 / 状态筛选 + 分页
- 查看任务结果：completed 状态可查看抓取到的文章列表
- 查看失败原因：failed 状态展示错误详情
- 任务可回溯：每条爬取数据关联 job_id，可查询"某次任务抓了哪些数据"

**爬取流程**（web 类型）：
1. 抓取列表页 HTML
2. 用 CSS 选择器提取文章标题、链接、摘要
3. 逐条进入详情页，用 Readability 算法自动提取正文
4. 从 meta 标签提取发布日期、关键词标签
5. 检测语言（中 / 英 / 俄 / 乌克兰语）
6. 结果写入 grab 表，关联 job_id

### 2.4 数据查看

浏览已爬取的数据，支持筛选和详情查看。

**功能点**：
- 分页列表（标题、摘要、数据源、语言、分类、标签、抓取时间）
- 按数据源筛选
- 按关键词搜索（标题 / 正文）
- 按语言筛选
- 点击查看详情（抽屉式面板，展示完整正文、元信息、原始链接）

### 2.5 数据导出

将已爬取的数据导出为多种格式。

**支持格式**：
- `json`：内联 JSON，浏览器直接下载
- `csv`：CSV 文件，写入后端 output/ 目录
- `docx`：格式化 Word 报告（封面 + 摘要统计表 + 按数据源分组 + 文章正文），写入 output/ 目录

## 3. 库表设计

### 3.1 source 表（数据源配置）

```sql
CREATE TABLE source (
    id               INT AUTO_INCREMENT PRIMARY KEY,
    name             VARCHAR(50)  NOT NULL UNIQUE COMMENT '数据源唯一名称',
    url              VARCHAR(500) NOT NULL COMMENT '目标网址',
    description      VARCHAR(200) NOT NULL DEFAULT '' COMMENT '描述',
    source_type      VARCHAR(20)  NOT NULL DEFAULT 'web' COMMENT '类型: web / rss / api',
    selectors        JSON         DEFAULT NULL COMMENT '类型相关配置（web=CSS选择器，api=endpoint/查询参数）',
    selector_source  VARCHAR(20)  NOT NULL DEFAULT 'manual' COMMENT '选择器来源: detector|preset|fallback|manual',
    enabled          TINYINT(1)   NOT NULL DEFAULT 1 COMMENT '是否启用',
    created_at       DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at       DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.2 scrape_job 表（爬取任务）

```sql
CREATE TABLE scrape_job (
    job_id       VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT '任务 UUID',
    source_id    INT          NOT NULL COMMENT '关联 source.id',
    source_name  VARCHAR(50)  NOT NULL COMMENT '数据源名称（冗余）',
    status       VARCHAR(20)  NOT NULL DEFAULT 'pending' COMMENT 'pending|running|completed|failed',
    limit_count  INT          NOT NULL DEFAULT 20 COMMENT '请求的抓取上限',
    total        INT          NOT NULL DEFAULT 0 COMMENT '实际入库条数',
    params       JSON         DEFAULT NULL COMMENT '调用参数快照',
    error        TEXT         DEFAULT NULL COMMENT '失败原因',
    created_at   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    started_at   DATETIME     DEFAULT NULL COMMENT '开始执行时间',
    completed_at DATETIME     DEFAULT NULL COMMENT '完成时间',
    FOREIGN KEY (source_id) REFERENCES source(id) ON DELETE CASCADE,
    INDEX idx_job_status (status),
    INDEX idx_job_created (created_at),
    INDEX idx_job_source (source_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.3 grab 表（爬取结果）

```sql
CREATE TABLE grab (
    id           INT AUTO_INCREMENT PRIMARY KEY,
    source_id    INT          NOT NULL COMMENT '关联 source.id',
    job_id       VARCHAR(36)  DEFAULT NULL COMMENT '关联 scrape_job.job_id',
    source_name  VARCHAR(50)  NOT NULL COMMENT '数据源名称（冗余）',
    title        VARCHAR(500) DEFAULT '' COMMENT '标题',
    content      MEDIUMTEXT   COMMENT '正文内容',
    summary      VARCHAR(1000) DEFAULT '' COMMENT '摘要',
    source_url   VARCHAR(500) DEFAULT '' COMMENT '原始URL',
    language     VARCHAR(10)  DEFAULT '' COMMENT '语言',
    category     VARCHAR(50)  DEFAULT '' COMMENT '分类',
    tags         JSON         DEFAULT NULL COMMENT '标签列表',
    published_at DATETIME     DEFAULT NULL COMMENT '发布时间',
    grabbed_at   DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '抓取时间',
    raw_json     JSON         DEFAULT NULL COMMENT '原始JSON数据',
    FOREIGN KEY (source_id) REFERENCES source(id) ON DELETE CASCADE,
    INDEX idx_source (source_name),
    INDEX idx_grabbed (grabbed_at),
    INDEX idx_job (job_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 4. 选择器配置规范

选择器配置存储在 source 表的 `selectors` JSON 字段中，字段结构按数据源类型区分。
本节明确 `web` 与 `api` 两种类型的字段含义、必填项、配置示例和调优建议。

### 4.1 web 类型选择器

**配置目标**：从 HTML 列表页提取文章条目，并定位详情页正文区域。

**字段定义**：

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `article_selector` | 是 | string | 列表页文章条目的容器选择器（可多选，逗号分隔） |
| `title_selector` | 是 | string | 容器内标题元素选择器（可多选） |
| `link_selector` | 是 | string | 容器内详情页链接选择器 |
| `content_selector` | 否 | string | 详情页正文容器选择器；留空则用 Readability 算法自动提取 |
| `date_selector` | 否 | string | 详情页发布时间选择器 |
| `summary_selector` | 否 | string | 列表页摘要选择器；留空则摘要在详情页提取时生成 |
| `tags_selector` | 否 | string | 详情页标签选择器 |
| `link_filter` | 否 | string | 链接过滤特征字符串，仅保留 href 含此串的链接（用于排除导航/广告） |
| `js_rendered` | 否 | bool | 是否为 JS 渲染页面，由 detector 自动写入；前端据此提示用户 |

**配置流程**：

1. **首选自动检测**：只填 `name + url` 创建数据源，系统调用 `detect_selectors()` 分析 HTML 结构，并通过 `validate_selectors()` 试抓取前 3 条验证有效性
2. **失败回退预设**：detector 未通过则按 hostname 匹配 `selector_presets.py` 中的预置模板（已内置 32 个主流站点）
3. **再失败用通用兜底**：未命中预设时用 `FALLBACK_SELECTORS`（`li, article, .post, .news-item, .entry` 等）
4. **手动微调**：在前端"选择器测试"抽屉中通过 `/api/sources/preview` 接口实时预览抓取效果，调整后保存即标记 `selector_source=manual`

**示例 1：新华网（预设命中）**

```json
{
    "article_selector": "li",
    "title_selector": "div.tit a",
    "link_selector": "div.tit a",
    "content_selector": "#detailContent",
    "date_selector": ".info"
}
```

**示例 2：人民网（无 h 标签，标题为纯 a）**

```json
{
    "article_selector": ".news-item, li",
    "title_selector": "h3 a, .title a",
    "link_selector": "a",
    "content_selector": ".rm_txt, .article, #p_content",
    "date_selector": ".time, .date"
}
```

**示例 3：Hacker News（用 link_filter 过滤 item?id= 链接）**

```json
{
    "article_selector": ".athing",
    "title_selector": ".titleline a, a",
    "link_selector": "a",
    "link_filter": "item?id=",
    "content_selector": "",
    "date_selector": ".age, [class*=time]"
}
```

**调优建议**：
- 列表页抓 0 条 → 检查 `article_selector` / `title_selector` 是否匹配当前页面结构，使用前端"选择器测试"按钮即时验证
- 正文提取失败（content 留空）→ 配置 `content_selector` 绕过 Readability
- 详情页 JS 渲染 → 系统会标记 `js_rendered: true`，前端提示建议配置专用 `content_selector`；彻底解决需引入无头浏览器（P2 规划）
- `content_selector` 支持多选（逗号分隔），系统按顺序取首个非空结果

### 4.2 api 类型选择器

**配置目标**：通过 JSON 接口获取结构化数据，无需 CSS 选择器。系统按是否配置 `endpoint` 字段区分两种子模式。

**字段定义**：

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `endpoint` | 否 | string | tRPC 过程名（如 `articles.list`）。**配置此字段即走 tRPC 模式** |
| 其他字段 | 否 | any | tRPC 模式下作为查询参数传入；REST 模式下当前忽略 |

**子模式 A：tRPC 模式**（如 RedroomCN 平台）

当 `selectors` 中含 `endpoint` 字段时，系统按 tRPC 协议请求 `{url}/api/trpc/{endpoint}`，其余字段作为查询参数。

```json
{
    "endpoint": "articles.list",
    "limit": 20
}
```

请求路径形如：`{url}/api/trpc/articles.list?batch=1&input={"0":{"json":{"limit":20}}}`，
响应由 `RedroomParser` 按 endpoint 前缀分派解析（`articles.*` / `agencies.*` / `facilities.*`）。

**子模式 B：通用 REST API 模式**

不配置 `endpoint` 时，系统直接 `GET {url}`，按以下字段自动从 JSON 中提取文章数据：

| 文章字段 | JSON 候选键（按优先级） |
|----------|-------------------------|
| title | `title` → `name` |
| summary | `summary` → `description`（截断 300 字） |
| content | `content` → `body` |
| source_url | `url` → `link`（缺失则用接口 URL） |
| language | `language` → `lang` |
| tags | `tags` → `keywords`（需为 list） |
| category | `category` → `type` |
| 列表根 | `data` → `results` → `items` → 顶层 list |

**示例 1：tRPC 模式（RedroomCN）**

```json
{
    "endpoint": "agencies.list",
    "limit": 50
}
```

**示例 2：REST API 模式（jsonplaceholder）**

```json
{}
```

或留空 `selectors`，系统直接 GET `{url}` 并从 `data` 数组提取。

**配置建议**：
- 普通第三方 REST API → `selectors` 留空或 `{}`，依赖系统自动识别字段
- 自建 tRPC 服务 → 配置 `endpoint` + 查询参数
- 避免误配置：REST API 不要传入 `endpoint` 字段，否则会走 tRPC 分支导致 404

### 4.3 类型自动检测

创建数据源时若不手动指定类型，系统按以下策略自动判定：

1. 请求 URL，若 `Content-Type: application/json` → 判定为 `api`，默认配置 `{"endpoint": "articles.list"}`
2. 尝试访问 `{url}/api/trpc/agencies.list`，若 200 → 判定为 `api`（tRPC 服务）
3. 否则 → 判定为 `web`，进入选择器自动检测流程

## 5. 选择器智能配置改进（已实施）

### 5.1 背景与目标

早期版本支持"只填 name + url"创建数据源，但存在三个核心短板：
1. **检测不验证**：`detect_selectors()` 只分析 HTML 结构生成选择器，不实际试抓取验证，可能产出"看起来对、实际抓不到"的选择器
2. **爬取时不兜底**：`GenericScraper` 只读取 `source.selectors`，字段为空或检测失败时直接回退默认 `a` 标签，导致只抓 1 条且多为噪声
3. **前端无闭环**：用户无法看到自动检测结果、无法手动微调、无法在保存前预览抓取效果

通过 P0（智能检测增强）和 P1（选择器测试预览）两阶段，将"只填 URL"的默认体验从"基本不可用"提升到"开箱可用 + 可调优"。

### 5.2 P0：智能检测增强（已完成）

| 编号 | 功能点 | 验收标准 | 实现位置 |
|------|--------|---------|---------|
| P0-1 | detector 增加"试抓取验证" | 检测后实际抓取前 3 条，标题长度 ≥ 6 且链接有效比例 ≥ 60% 才视为通过 | `selector_detector.validate_selectors()` |
| P0-2 | GenericScraper 兜底集成 | `selectors` 为空或 `article_selector` 缺失时，自动调用 `detect_selectors(url)`，再失败用 `get_selectors(url)` | `generic_scraper._ensure_selectors()` |
| P0-3 | presets 模板库扩充至 30+ 站点 | 覆盖国内/国外主流新闻 + 科技社区共 32 个站点 | `selector_presets.PRESETS` |
| P0-4 | 检测元数据记录 | source 表新增 `selector_source` 字段，创建/更新时写入 | `storage/database.source_create/update` |
| P0-5 | JS 渲染页面降级标注 | 检测到 `<noscript>` 或正文 < 150 字时标记 `js_rendered: true`，前端提示 | `selector_detector._detect_js_rendered()` |
| P0-6 | 爬取时选择器失效自动重试 | GenericScraper 用当前 selectors 抓到 0 条时，自动重新检测并重试 1 次 | `generic_scraper.scrape()` |

**关键设计**：

- **四级选择器兜底链**：`manual（用户传入）→ detector（自动检测+验证）→ preset（域名匹配）→ fallback（通用兜底）`，在 `_ensure_selectors()` 中实现
- **试抓取双重验证**：detector 检测出的选择器需通过 `validate_selectors()` 实际抓取前 3 条校验，避免"结构正确但语义错误"
- **0 条自动重试**：首次抓取 0 条时自动调用 detector 重新检测并重试，提升抓取成功率

### 5.3 P1：选择器测试预览（已完成）

| 编号 | 功能点 | 验收标准 | 实现位置 |
|------|--------|---------|---------|
| P1-1 | 新增 `POST /api/sources/preview` 接口 | 输入 url + 可选 selectors，返回前 3 条抓取预览 | `server/routes/sources.api_preview_source` |
| P1-2 | 接口支持"仅 URL"模式 | selectors 为空时自动调用 detector，返回检测结果 + 试抓样本 | 同上 |
| P1-3 | 接口支持"URL + selectors"模式 | 用户传入 selectors 时跳过检测，直接用传入选择器试抓 | 同上 |
| P1-4 | 前端 SourcesView 新增"选择器测试"按钮 | 数据源列表每行加"测试"操作，点击弹出预览抽屉 | `SourcesView.vue` |
| P1-5 | 前端新增数据源表单支持手动填写 selectors | 创建/编辑表单增加 selectors JSON 编辑器（折叠区，默认收起） | `SourcesView.vue` |
| P1-6 | 前端创建时展示自动检测结果 | 创建成功后 toast 提示选择器来源和验证结果 | `SourcesView.vue` |
| P1-7 | 前端预览抽屉展示抓取样本 | 左侧 3 条样本卡片，右侧当前 selectors JSON + 验证结果 | `SourcesView.vue` |

**API 设计**：

```http
POST /api/sources/preview
Content-Type: application/json

{
    "url": "https://www.example.com",
    "selectors": {
        "article_selector": "li.news-item",
        "title_selector": "h3 a",
        "link_selector": "a",
        "content_selector": ".article-body"
    },
    "sample_size": 3
}
```

`selectors` 可选，为空时触发自动检测；`sample_size` 默认 3，最大 5。

**响应**：

```json
{
    "success": true,
    "url": "https://www.example.com",
    "selector_source": "detector",
    "selectors": {
        "article_selector": "li.news-item",
        "title_selector": "h3 a",
        "link_selector": "a",
        "content_selector": ".article-body",
        "date_selector": "time"
    },
    "js_rendered": false,
    "samples": [
        {
            "title": "样本文章标题 1",
            "url": "https://www.example.com/news/2026-08-12/story-1",
            "summary": "摘要前 200 字...",
            "content_preview": "正文前 200 字...",
            "content_length": 1523,
            "published_at": "2026-08-12"
        }
    ],
    "validation": {
        "total": 3,
        "valid": 3,
        "passed": true
    },
    "elapsed_ms": 1850
}
```

### 5.4 实施覆盖率

| 改进点 | 对应需求 | 覆盖状态 |
|--------|---------|---------|
| 只填 URL 默认体验提升 | P0-1/2/3/6 | ✅ 完整覆盖 |
| 选择器可手动微调 | P1-5 | ✅ 完整覆盖 |
| 保存前预览效果 | P1-1/2/3/4/7 | ✅ 完整覆盖 |
| 常见站点一键可用 | P0-3 | ✅ 完整覆盖 |
| JS 渲染页面识别 | P0-5 | ✅ 完整覆盖 |
| 检测来源可追溯 | P0-4 | ✅ 完整覆盖 |
| Playwright JS 渲染 | — | ❌ 不在本期范围（P2 后续） |

### 5.5 不在本期范围

以下改进不纳入 P0/P1，作为 P2 后续规划：

- **Playwright/Selenium 无头浏览器**：彻底解决 JS 渲染页面，成本较高，待 P0/P1 验证后评估
- **RSS 类型数据源支持**：与选择器配置无关，独立需求
- **选择器模板管理后台**：presets 目前硬编码，后续可改为数据库管理
- **多语言选择器规则库**：按语言区域定制 detector 策略

## 6. 爬取效果测试报告

### 6.1 测试环境

- 测试时间：2026-08-12
- 后端：FastAPI + MySQL，localhost:8000
- 测试数据源：xinhua（新华网）、people（人民网）、今日俄罗斯（RT）、环球网、json-api

### 6.2 测试结果

| 数据源 | 类型 | 状态 | 抓取条数 | 正文质量 | 语言检测 | 问题 |
|--------|------|------|----------|----------|----------|------|
| 新华网 | web | completed | 5/5 | 部分好部分差 | ❌ 全 en | 部分正文仅 9 字（=标题） |
| 人民网 | web | completed | 0/5 | - | - | 选择器不匹配，0 条 |
| 今日俄罗斯 | web | completed | 5/5 | ✅ 优秀 | ✅ en | 无 |
| 环球网 | web | completed | 5/5 | ❌ 差 | ❌ 全 en | 正文=标题，Readability 提取失败 |
| json-api | api | failed | 0/5 | - | - | 误走 tRPC 模式，404 |

### 6.3 问题分析与解决方案

#### 问题 1：语言检测全部为 en（严重，已修复）

**现象**：中文网站（新华网、环球网）的数据 language 字段全部为 `en`。

**根因**：GenericScraper 创建 NewsArticle 时从未调用 `detect_language()`，NewsArticle.language 默认值为 `"en"`，被直接写入数据库。

**解决方案**：在 GenericScraper.scrape() 末尾对每篇文章调用语言检测：
```python
from utils.language_detector import detect_language
for article in articles:
    article.language = detect_language(article.content or article.title)
```

#### 问题 2：环球网正文=标题（严重，已修复）

**现象**：环球网 5 条数据的正文长度等于标题长度，说明 Readability 算法提取正文失败。

**根因**：环球网详情页（opinion.huanqiu.com/article/xxx）可能使用 JavaScript 动态渲染正文，httpx 只拿到初始 HTML，正文区域为空或只有标题。Readability 算法在缺少正文 DOM 的情况下回退返回了标题文本。

**解决方案**：
- 详情页提取失败时 content 留空，记录警告日志，不再用标题填充
- 配置专用 `content_selector` 绕过 Readability
- 通过 P0-5 检测 JS 渲染并标记 `js_rendered: true`，前端提示

#### 问题 3：人民网 0 条（中等，已修复）

**现象**：人民网首页找到 289 个容器，但标题选择器 `h1, h2, h3 a` 未匹配到任何文章，提取 0 条。

**根因**：人民网首页使用 `<a>` 标签包裹标题文本，没有 `h1/h2/h3` 标签。默认选择器不适用。

**解决方案**：为人民网配置正确的 CSS 选择器（已纳入 presets）：
```json
{
    "article_selector": ".news-item, li",
    "title_selector": "h3 a, .title a",
    "link_selector": "a",
    "content_selector": ".rm_txt, .article, #p_content",
    "date_selector": ".time, .date"
}
```

#### 问题 4：新华网部分正文短（中等，已修复）

**现象**：第一条 239 字正文（正常），第二条仅 9 字（=标题长度）。

**根因**：部分详情页可能返回了不同结构（如图片新闻、视频新闻），Readability 算法在这些页面上提取失败，回退用了标题。

**解决方案**：
- 在详情页提取失败时记录警告日志，标记 `content` 为空而非用标题填充
- 为新华网配置 `content_selector` 作为 Readability 的备选

#### 问题 5：摘要=标题（中等，已修复）

**现象**：列表页无摘要选择器时用标题填充 summary，详情页提取失败时 summary 未更新。

**根因**：`generic_scraper.py` 中 `summary=summary or title` 用标题兜底；详情页失败时没有更新摘要。

**解决方案**：
- 列表页无摘要时留空，不用标题填充
- 详情页成功时用 `extract_summary()` 更新摘要
- 详情页失败时保持摘要为空，前端展示"暂无摘要"

#### 问题 6：标签/分类提取不准（轻微，规划中）

**现象**：新华网第二条标签包含"赵薇"、"王子"等无关内容，分类为"翊"。

**根因**：`extract_tags()` 从 meta keywords 提取，但部分网站的 meta keywords 包含编辑名、设计者名等非内容标签。`category = tags[0]` 直接取第一个标签作为分类，不准确。

**解决方案**：
- 过滤明显非内容标签（人名、单字、编辑/设计/策划等关键词）
- category 独立提取（从 meta article:section 或面包屑导航），不取 tags[0]

#### 问题 7：json-api 误走 tRPC 模式（轻微，已规避）

**现象**：json-api 数据源的 selectors 配置了 `{"endpoint": "articles.list"}`，导致 `_scrape_api()` 误走 tRPC 分支，URL 拼接为 `jsonplaceholder.typicode.com/posts/api/trpc/articles.list`，返回 404。

**根因**：api 类型的分派逻辑只检查 selectors 是否有 endpoint 字段，未区分 tRPC 服务和普通 REST API。

**解决方案**：按 §4.2 规范配置——REST API 不传 `endpoint` 字段，留空 `selectors` 即走通用 REST 模式。

### 6.4 改进优先级

| 优先级 | 问题 | 预期效果 | 状态 |
|--------|------|----------|------|
| P0 | 语言检测 | 中文网站数据正确标记为 zh | ✅ 已修复 |
| P0 | 环球网正文提取 | 配置专用选择器，正文不再=标题 | ✅ 已修复 |
| P0 | 选择器智能检测增强 | 试抓取验证 + 兜底链 + 32 站点预设 | ✅ 已完成（见 §5.2） |
| P0 | 选择器测试预览 | /preview 接口 + 前端预览抽屉 | ✅ 已完成（见 §5.3） |
| P1 | 人民网选择器 | 能正常提取文章列表 | ✅ 已修复（纳入 presets） |
| P1 | 摘要兜底逻辑 | 摘要不再=标题，留空等待详情页 | ✅ 已修复 |
| P2 | 标签/分类过滤 | 过滤人名等非内容标签 | ⏳ 规划中 |
| P2 | Playwright JS 渲染 | 彻底解决 JS 渲染页面 | ⏳ 规划中 |

## 7. 风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| detector 对部分站点结构误判 | 中 | 抓到噪声或 0 条 | P0-1 试抓验证 + P0-6 自动重试 + P1 手动微调三重保险 |
| presets 选择器随网站改版失效 | 低 | 抓取 0 条 | P0-6 自动重试切到 detector，且前端测试按钮可即时发现 |
| preview 接口响应慢（详情页多） | 中 | 用户体验差 | sample_size 限制 ≤ 5，详情页并行抓取，超时 5 秒 |
| 用户手动填写 selectors 语法错误 | 中 | 抓取失败 | 前端 JSON 校验 + 后端 try/except 兜底 |
| JS 渲染页面无法用 httpx 抓取 | 中 | 正文为空 | P0-5 标记 js_rendered 提示用户配置专用 content_selector；P2 引入无头浏览器 |
